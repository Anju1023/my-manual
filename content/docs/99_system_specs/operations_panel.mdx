---
title: '操作シート・ボタン機能'
description: Excelの「操作」シートにある各ボタンの機能と、実行されるマクロの対応表です。
---

import { Callout } from 'fumadocs-ui/components/callout';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { MousePointerClick, Play, Printer } from 'lucide-react';

# 🎛️ 操作シート・ボタン機能一覧

「操作」シートに配置されているボタンと、実行される VBA マクロの対応表です。
ボタンの配置が変わったり、機能を追加する際の参考にしてください。

## 🚀 一括出力系 (メイン機能)

日常業務で主に使用する、複数の処理をまとめて行うボタンです。

<TypeTable
	type={{
		全部まとめて出力: {
			description:
				'「製造数」「ラベル」「生地出し」「発注」をすべて一括で実行します。土曜日の更新後に使用します。',
			type: '全部まとめて出力',
		},
		合計まとめて出力: {
			description:
				'「CB 製造数」「クリエイト製造数」「製造数(合計)」の 3 シートを再集計します。',
			type: '合計まとめて出力',
		},
		まとめてラベル出し出力: {
			description:
				'ローゼン用とクリエイト用の「消費期限チェック表」を一括出力します。',
			type: 'まとめてラベル出し出力',
		},
		生地出し並べ表まとめて出力: {
			description:
				'「冷凍生地」「スクラッチ」「並べる表」の 3 つを一括出力します。',
			type: '生地出し並べ表まとめて出力',
		},
	}}
/>

## 🏭 個別出力系 (メンテナンス用)

特定の帳票だけを再出力したい場合に使用します。

### 製造数・集計

<TypeTable
	type={{
		'CB 製造数出力': {
			description: 'ローゼン店舗分の集計',
			type: 'CB製造数合計出力',
		},
		クリエイト製造数出力: {
			description: 'クリエイト納品分の集計',
			type: 'クリエイト製造数合計出力',
		},
		製造数出力: {
			description: '上記 2 つを合算した総製造数',
			type: '製造数合計出力',
		},
	}}
/>

### 工程表・ラベル

<TypeTable
	type={{
		冷凍生地出し表出力: {
			description: '前日解凍が必要な生地のリスト',
			type: '冷凍生地出し表出力',
		},
		スクラッチ生地出し表出力: {
			description: '当日仕込み（スクラッチ）生地のリスト',
			type: 'スクラッチ生地出し表出力',
		},
		並べる表出力: {
			description: '天板への配置数（〇枚 × 〇個）を計算',
			type: '並べる表出力',
		},
		ラベル出し表出力: {
			description: 'ラベラー登録用の個数表',
			type: '消費期限チェック表出力',
		},
	}}
/>

### 発注・納品

<TypeTable
	type={{
		発注表出力: {
			description: '原材料の所要量を計算し、仕入先ごとにシート分けして出力',
			type: '発注表作成 ➔ 発注表_仕入先別出力',
		},
		'商品別数量表(ローゼン)': {
			description: '店舗ごとの仕分けリスト（別ブック作成）',
			type: '商品別シート出力',
		},
		'商品別数量表(クリエ)': {
			description: 'クリエイト用の仕分けリスト（別ブック作成）',
			type: 'クリエイト商品別シート出力',
		},
	}}
/>

## ⚙️ システム制御系

<TypeTable
	type={{
		更新: {
			description:
				'【重要】土曜日に実行。翌週分のデータを今週分にコピーし、日付を 1 週間進めます。',
			type: '更新',
		},
		新規入力: {
			description: '新商品登録用のユーザーフォームを起動します。',
			type: 'frmInput.Show',
		},
	}}
/>

<Callout type="info" title="開発者メモ" icon={<MousePointerClick />}>
	**「更新」ボタンの挙動について** 「更新」マクロは、各シートの
	`J列〜P列`（翌週エリア）を
	`C列`（今週エリア）に値貼り付けする処理を行っています。
	そのため、**実行前に[バックアップ](../04_ad_hoc/index.mdx)を取らないと、今週分のデータが消えて復元できなくなります。**
</Callout>
